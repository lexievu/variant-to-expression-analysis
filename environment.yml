name: biotech_challenge
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - cyvcf2=0.31.4
  - numpy
  - pandas
  - pip
  - pip:
      - pytest
      - python-dotenv
      - alphagenome
